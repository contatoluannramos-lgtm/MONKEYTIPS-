// @ts-nocheck
import React from 'react';

/**
 * ⚠️ ARQUIVO DEPRECADO ⚠️
 * 
 * A lógica da aplicação foi movida para o App.tsx na raiz do projeto para corrigir
 * problemas de resolução de caminhos de importação.
 * 
 * Este arquivo é mantido apenas para satisfazer ferramentas de build que exigem
 * uma entrada em src/, mas não contém lógica.
 */

export default function App() {
  return null;
}
